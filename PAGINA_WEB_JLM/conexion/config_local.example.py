# Este es un archivo de EJEMPLO, no contiene una contrasena real.
# Cada integrante debe crear su propio archivo conexion/config_local.py
# (sin ".example" en el nombre) con su contrasena real de PostgreSQL.
# Ese archivo config_local.py esta en .gitignore y nunca se sube al repositorio.

PASSWORD = 'escribe_aqui_tu_contrasena_local'