# config_local.py
# Este es un archivo de EJEMPLO, no contiene una contraseña real.
# Cada integrante debe crear su propio archivo conexion/config_local.py
# (sin ".example" en el nombre) con su contraseña real de PostgreSQL.
# Ese archivo config_local.py esta en .gitignore y nunca se sube al repositorio.

HOST = "localhost"
USER = "postgres"
PASSWORD = "CONTRASEÑA"  # <- Aquí va la contraseña real de PostgreSQL de cada integrante
DATABASE = "jlmconnect"
PORT = 5432